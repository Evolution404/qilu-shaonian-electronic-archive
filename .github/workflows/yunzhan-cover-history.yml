name: YunZhan Qilu Shaonian Cover History

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/recover_yunzhan_cover_history.py'
      - '.github/workflows/yunzhan-cover-history.yml'

permissions:
  contents: write

concurrency:
  group: yunzhan-cover-history
  cancel-in-progress: true

jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install image dependency
        run: python -m pip install --disable-pip-version-check pillow
      - name: Recover fixed-image history
        run: python scripts/recover_yunzhan_cover_history.py
      - name: Commit history metadata
        run: |
          if [ -z "$(git status --porcelain -- data/yunzhan_cover_history)" ]; then exit 0; fi
          git config user.name 'qilu-archive-bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/yunzhan_cover_history
          git commit -m 'data: recover YunZhan Qilu Shaonian image history'
          for attempt in 1 2 3 4; do
            git fetch origin main
            git rebase origin/main
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
