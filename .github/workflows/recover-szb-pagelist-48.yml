name: Recover SZB Pagelist 48

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/recover_szb_pagelist_48.py'
      - '.github/workflows/recover-szb-pagelist-48.yml'

permissions:
  contents: write

concurrency:
  group: recover-szb-pagelist-48
  cancel-in-progress: true

jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    env:
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Recover verified Pagelist API
        run: python scripts/recover_szb_pagelist_48.py
      - name: Commit recovered page metadata
        run: |
          if [ -z "$(git status --porcelain -- data/szb_pagelist_48)" ]; then exit 0; fi
          git config user.name 'qilu-archive-bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/szb_pagelist_48
          git commit -m 'data: record 2021 pagelist API recovery'
          for attempt in 1 2 3 4; do
            git fetch origin main
            git rebase origin/main
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
